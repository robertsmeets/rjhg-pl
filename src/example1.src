class myclass
{
    a,b,c,d,e
}

proc myproc(e,f,g,h)
{
   z=1;
   b=2;
   c=3;
   while (z > 10)
   {
      z=z+1;
      b=b*2;
   };
   if (z > b)
   {
     c = p + b;
   }
   else
   {
     a = b*b;
   };
   return z;
}

proc main(a)
{
   print "HELLO";
   m = myclass();
   f = m.mymethod(1,2,3);
   print f;
}


method myclass.mymethod(x,y,z)
{
   print "INTHEMETHOD";
   a = x + y + z;
   b = x * y * z;
   if (a > b)
   {
     c = a + b;
   };
   return c;
}

