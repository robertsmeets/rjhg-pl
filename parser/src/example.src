class myclass
{
    a,b,c,d,e;
}

proc myproc(e,f,g,h)
{
   z=1;
   b=2;
   c=3;
   while (z > 10)
   {
      z=z+1;
      b=b*2;
   }
   if (z > b)
   {
     c = p + b(x);
   }
   else
   {
     a = a * a;
   }
   return z;
}

method myclass.mymethod(x,y,z)
{
   a = x + y + z;
   b = x * y * z;
   if (a > b)
   {
     c = a + b;
   }
   return c;
}

